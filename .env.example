# Example environment configuration for CardApp
# Copy to .env (frontend) or export variables in shell as needed.

# --- Frontend ---
VITE_API_BASE=http://localhost:5000
VITE_FRONTEND_PORT=5173

# Tema iniziale (light | dark)
COLOR_MODE=light

# --- Backend / API ---
# (If backend supports reading from environment - otherwise document only)
BACKEND_PORT=5000

# --- Test Mode Flags ---
# Abilita mock interno in-memory (nessun backend necessario per alcuni test)
INTERNAL_API_TEST_MOCK=0
# Riabilita polling durante i test (normalmente disabilitato per ordine fetch deterministico)
ENABLE_POLL_IN_TEST=0

# --- Optional Future Flags ---
# FEATURE_WEBSOCKET=0
# FEATURE_VERBOSE_LOG=0
FEATURE_DARK_MODE=1
FEATURE_DEBUG_PANEL=1

# NOTE: Non committare file .env con credenziali reali.
